package XPVs;

//import fileworks.*;
public class files {
  /*  static int randomNumber(int min, int max) {
        return (int)(Math.random() * (max - min + 1) + min);
    }

    public static void main(String[] args) {
        DataExport de = new DataExport("output.txt");
        for (int i = 0; i < 20; i++) {
            de.writeLine(String.valueOf(randomNumber(1, 100)));
        }
        de.finishExport();



        DataImport di = new DataImport("output.txt");
        int number;
        int min = Integer.MAX_VALUE;
        while (di.hasNext()) {
            number = Integer.parseInt(di.readLine());
            if (min > number) {
                min = number;
            }
        }
        System.out.println(min);
        di.finishImport();






       /* de.writeLine("Hello world!");
        de.writeNewLine();
        de.writeString("Dalsi radka");



       de.finishExport();





        DataImport di = new DataImport("input.txt");
        di.printFile(); //vypise cely soubor

        while (di.hasNext()) {
        //    System.out.print(di.readChar()); //nactu po jednotlivych znacich
            System.out.println(di.readLine());//nactu po radkach
        }

        di.finishImport(); //pouzivat vzdy (ukoncuje cteni)


    }*/
}
